# meta/plugin_routing.yml  
plugin_routing:
  modules:
    fmc_facts:
      redirect: cisco.fmcansible.fmc_facts
  
  # Network OS routing for gather_facts
  network_os:
    cisco.fmcansible.fmc:
      facts: cisco.fmcansible.fmc_facts
    fmc:
      facts: cisco.fmcansible.fmc_facts
